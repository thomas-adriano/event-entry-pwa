import { PageTransitionDirective } from './page-transition.directive';

describe('PageTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new PageTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
